name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        ocaml-compiler: ["4.14", "5.1", "5.4"]

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v7

      - uses: ocaml/setup-ocaml@v3
        with:
          ocaml-compiler: ${{ matrix.ocaml-compiler }}

      # ucharset is not on opam yet. Drop this once it is published.
      - run: opam pin add ucharset https://github.com/enetsee/ucharset.git --no-action

      - run: opam install . --deps-only --with-test

      - run: opam exec -- dune build

      - run: opam exec -- dune runtest

  lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - uses: ocaml/setup-ocaml@v3
        with:
          ocaml-compiler: "5.4"

      # pinned in .ocamlformat, so this checks the exact version the repo uses
      - uses: ocaml/setup-ocaml/lint-fmt@v3

      - uses: ocaml/setup-ocaml/lint-doc@v3

      - uses: ocaml/setup-ocaml/lint-opam@v3
